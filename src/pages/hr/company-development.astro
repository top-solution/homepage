---
import Layout from "../../components/Layout.astro";
import TopBanner from "../../components/TopBanner.astro";
import Button from "../../components/Button.astro";
import Hex from "../../components/Hex.astro";
import Customers from "../../components/Customers.astro";
import HeaderHexagonsDesktop from "../../components/self-development/HeaderHexagonsDesktop.astro";
import HeaderHexagonsMobile from "../../components/self-development/HeaderHexagonsMobile.astro";
import CardHexGroup from "../../components/self-development/CardHexGroup.astro";
import Card from "../../components/Card.astro";
import { companyDevelopmentCard } from "../../utils/cards";
---

<Layout color="light" subPages={true}>
  <TopBanner title="Sviluppo organizzativo" mobilealignleft="true" title2={true}>
    <p class="claim" slot="text">
      è necessario ascoltare la voce delle proprie persone per capire come costruire azioni di miglioramento e con loro
      progettare il futuro
    </p>
    <div slot="actions" style="
      display: flex;
      flex-direction: column;
      max-width: 175px;
      margin: auto;
    ">
      <Button variant="primary" href="/contacts/">Contattaci</Button>
    </div>
    <div slot="external">
      <HeaderHexagonsDesktop />
      <HeaderHexagonsMobile />
      <div class="floating-hex-0">
        <Hex width="56" fill="#9893c1" shadow="true" />
      </div>
      <div class="floating-hex-3">
        <Hex width="56" fill="#9893c1" shadow="true" />
      </div>
    </div>
  </TopBanner>
  <section>
    <div class="copy">
      <p>
        Per affrontare situazioni organizzative ritenute critiche occorre analizzare i processi organizzativi e
        identificare i meccanismi di successo interni da replicare o apportare azioni correttive ove richiesto.
      </p>
      <video controls>
        <track kind="captions" />
        <source src="../../videos/company-development.mp4" type="video/mp4" />
      </video>
    </div>
    <div class="services">
      <h3>
        I nostri <b>SERVIZI</b>
      </h3>
      <div class="card-container">
        {companyDevelopmentCard.map((card) =>
        <Card title={card.title} description={card.description} />
        )}
        <div class="card-hex-group">
          <CardHexGroup />
        </div>
      </div>
      <Customers title="Chi ci ha scelto" />
    </div>
  </section>
</Layout>

<style>
  .floating-hex-2 {
    position: absolute;
    bottom: -200px;
    left: 50%;
    transform: translateX(-50%);
  }

  .floating-hex-3 {
    position: absolute;
    left: 20%;
    top: 370px;
  }

  .floating-hex-4 {
    position: absolute;
    left: 60%;
    top: 470px;
  }

  .floating-hex-5 {
    position: absolute;
    left: 65%;
    top: 600px;
  }

  .card-hex-group {
    position: absolute;
    left: 50%;
    top: 1200px;
  }

  .copy p {
    text-align: center;
    margin: var(--spacing-5) 0 var(--spacing-6);
    font-size: 22px;
  }

  .services {
    margin: var(--spacing-5) 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
  }

  video {
    width: 100%;
    height: auto;
    margin: var(--spacing-3) 0;
  }

  .card-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin: 0 var(--spacing-10);
  }

  h3 {
    margin-bottom: var(--spacing-6);
  }

  @media only screen and (max-width: 600px) {
    .copy {
      margin: 0 var(--spacing-2);
    }

    .floating-hex-3 {
      position: absolute;
      left: 0%;
      top: 490px;
    }

    .card-hex-group,
    .floating-hex-0,
    .floating-hex-3,
    .floating-hex-5 {
      display: none;
    }
  }

  @media screen and (max-width: 900px) {
    .card-hex-group {
      display: none;
    }
  }

  @media only screen and (max-width: 820px) {

    .card-hex-group {
      left: 50%;
      top: 675px;
    }
  }
</style>