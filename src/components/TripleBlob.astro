---
import { Icon } from 'astro-icon'
import Button from './Button.astro';
import Blob from './Blob.astro';
const { title, blobs, buttonlink } = Astro.props;
---

<section>
  <h1>{title}</h1>
  <div class="container">
    {blobs.map((blob) => (
    <a href={blob.href}>
      <Blob interactive={true} shape="rectangle" style={`max-width: ${blob.width};`} fill={blob.color} padding="20"
        clickable="true">
        <div class="text">
          <p>
            <img src={`../img/icons/${blob.icon}`} alt="devices icon" width="50" height="35" />
          </p>
          <p class="claim">
            <b>{blob.title}</b>
          </p>
          <h6>
            {blob.subtitle}
          </h6>
        </div>
      </Blob>
    </a>
    ))}
  </div>
  <div class="container-mobile">
    {blobs.map((blob) => (
    <a href={blob.href} style={`background-color: ${blob.colorMobile}`}>
      <h5>{blob.title}</h5>
      <Icon pack="mdi" name="arrow-right" />
    </a>
    ))}
  </div>
  <div class="centred-button">
    <Button variant="outlined" href={buttonlink} type="button">
      Scopri
    </Button>
  </div>
</section>

<style>
  .container {
    display: flex;
    justify-content: space-between;
    margin-bottom: var(--spacing-10);
    color: var(--color-blue);
  }

  .container-mobile {
    display: none;
  }

  a {
    margin: 0 var(--spacing-2);
    min-width: 260px;
  }

  .text {
    text-align: center;
  }

  p {
    margin-bottom: var(--spacing-1);
  }

  h1 {
    margin-top: var(--spacing-20)
  }

  @media only screen and (max-width: 960px) {

    .container,
    .centred-button {
      display: none;
    }

    .container-mobile {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    a {
      display: flex;
      align-items: center;
      width: 327px;
      height: 90px;
      border-radius: 4px;
      margin: var(--spacing-2) 0;
      padding: var(--spacing-2);
      justify-content: space-between;
    }

    h5 {
      color: var(--color-white);
      flex: 0 0 80%;
    }

    svg {
      color: var(--color-white) !important;
      width: 32px;
      height: 32px;
    }
  }
</style>